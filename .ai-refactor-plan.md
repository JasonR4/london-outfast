# Quote/Configurator to Brief Refactoring Plan

## Status: IN PROGRESS

### Completed ✅
1. Route definitions in App.tsx - `/quote` and `/configurator` now redirect to `/brief`
2. Navigation links in components
3. Global settings JSON (public/api/global-settings.json)
4. AMP page (public/amp/index.html)
5. Button navigation across 25 page components

### In Progress 🚧
#### User-Facing Text Updates Needed:
- "Get Quote" buttons → "Submit Brief" or "Get Started"
- "Quote form" references → "Brief form"
- "Your Quote" → "Your Brief" or "Your Campaign"
- "Configurator" text → "Brief Builder" or "Campaign Brief"

#### Files Requiring Text Updates (Priority Order):
1. **High Priority Pages** (89 matches found):
   - src/pages/FormatDirectory.tsx - "Request a Quote" buttons
   - src/pages/FormatPage.tsx - "Get Instant Quote" buttons
   - src/pages/ClientPortal.tsx - "Your Quotes & Campaigns" section
   - src/pages/CreateAccount.tsx - "Secure Your Quote" headings
   - src/pages/HtmlSitemap.tsx - sitemap entry

2. **Components** (40 matches):
   - src/components/SmartQuoteForm.tsx → rename/refactor
   - src/components/QuoteForm.tsx → rename/refactor  
   - src/components/QuoteFormSection.tsx → rename/refactor
   - src/components/QuoteBreakdown.tsx - keep (shows pricing breakdown)
   - src/components/QuoteSubmissionForm.tsx → rename/refactor
   - src/components/ConfiguratorTeaser.tsx - update text

3. **Documentation** (needs updating):
   - ANALYTICS_SETUP.md - analytics tracking references
   - README.md - feature descriptions
   - docs/flows/submit-auth-flow.md - flow documentation

4. **Analytics & Tracking**:
   - src/utils/analytics.ts - `trackQuoteSubmission` function
   - All analytics event names

### Functionality Preservation Rules:
- ✅ All routes must work exactly the same
- ✅ Form submissions must go to same endpoints  
- ✅ Data structure must remain unchanged
- ✅ Analytics tracking must continue working
- ✅ HubSpot integration must function identically

### Note:
The term "quote" in backend contexts (database tables, API responses, data structures) should remain unchanged to avoid breaking existing data and integrations. Only user-facing text should change.
